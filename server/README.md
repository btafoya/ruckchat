# ruckchat-server

RuckChat server crate. It will contain the REST API, WebSocket server, and MCP
server once those phases are implemented. The crate currently links the workspace
shared crates and migrations crate so that the workspace compiles end-to-end.
